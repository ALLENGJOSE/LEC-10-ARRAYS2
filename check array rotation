public class Solution {

    public static int arrayRotateCheck(int[] arr){
    	//Your code goes here
        int ans = 0;
        for(int i = 0 ; i < arr.length - 1 ; i++){
            if (arr[i + 1] <arr[i]){
                ans = (i + 1);
            }
        }
        return ans;
    }
}
